import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.artstudiomanager.app',
  appName: 'ArtStudioManager',
  webDir: 'dist/art-studio-manager.client'
};

export default config;